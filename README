Due to the size constraint, training and testing video are not included.

Deliverable test.txt is already in the current folder, to run the code, open main.ipynb run from block to block, then place train and test video file under data folder. 

total runtime is around 30 minutes on my laptop(should be a lot faster with cuda enabled Nvidia computer
